spring:
  application:
    name: FreelanceMarketplace
  datasource:
    url: jdbc:postgresql://localhost:5432/mydb
    username: myuser
    password: mypassword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

# JWT Configuration
jwt:
  secret: "uHvBi+hU1eL2n3IhlNKKVyH5SfREdnleiRfcN2AWRa8=" # Generate via: openssl rand -base64 32
  expiration: 86400000 # 24 hours

# Notification email
notification:
  email: "notifications@freelance.marketplace.com"